# Backend environment variables (reference). Local dev values live in the
# Tiltfile; real secrets should go in a gitignored backend/.env that the
# Tiltfile auto-loads.

DATABASE_URL=postgres://tally:password@localhost:5433/tally?sslmode=disable
SUPABASE_URL=https://aagdtgsmjpmiqtdbdquj.supabase.co
PORT=8081
